# Chtest Backend

This directory is reserved for the FastAPI backend.

Slice 1 only defines the container placeholder and Docker Compose service. Slice
2 adds the FastAPI app, typed settings, database session, Redis client, Alembic,
health checks, and single-user context.
